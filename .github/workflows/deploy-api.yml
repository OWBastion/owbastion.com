name: Deploy API

on:
  pull_request:
    paths:
      - 'apps/api/**'
      - 'packages/**'
      - 'migrations/**'
      - 'tools/**'
      - 'wrangler.toml'
      - 'wrangler.staging-d1.toml'
      - 'docs/api/openapi.json'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'tsconfig.json'
      - 'vitest.config.ts'
      - '.github/workflows/deploy-api.yml'
  push:
    branches: [main]
    paths:
      - 'apps/api/**'
      - 'packages/**'
      - 'migrations/**'
      - 'tools/**'
      - 'wrangler.toml'
      - 'wrangler.staging-d1.toml'
      - 'docs/api/openapi.json'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'tsconfig.json'
      - 'vitest.config.ts'
      - '.github/workflows/deploy-api.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: >-
    ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  PNPM_VERSION: 11.11.0
  NODE_VERSION: 26

jobs:
  validate:
    name: Validate (${{ matrix.name }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: migration checks
            command: pnpm run check:migrations
          - name: unit tests
            command: pnpm run test:unit
          - name: Portal UI tests
            command: pnpm run test:portal-ui --run
          - name: typecheck
            command: pnpm run typecheck
          - name: API build
            command: pnpm run build:api
          - name: Portal build and SSR smoke
            command: >-
              pnpm run build:portal && NUXT_TEST_PORT=3000
              pnpm run test:portal-e2e:built
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/action-setup@v6
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run ${{ matrix.name }}
        run: ${{ matrix.command }}

  verify:
    name: Verify platform
    if: always()
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - name: Require all validation checks to pass
        run: test "${{ needs.validate.result }}" = success

  deploy:
    name: Deploy Cloudflare Worker
    if: github.event_name != 'pull_request'
    needs: verify
    runs-on: ubuntu-latest
    environment: production
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_ZONE_ID: ${{ secrets.CLOUDFLARE_ZONE_ID }}
      QQBOT_API_TOKEN: ${{ secrets.QQBOT_API_TOKEN }}
      BASTION_BUILD_TOKEN: ${{ secrets.BASTION_BUILD_TOKEN }}
      QQBOT_POLICY_WEBHOOK_URL: ${{ secrets.QQBOT_POLICY_WEBHOOK_URL }}
      QQBOT_POLICY_WEBHOOK_SECRET: ${{ secrets.QQBOT_POLICY_WEBHOOK_SECRET }}
      OCRKIT_API_TOKEN: ${{ secrets.OCRKIT_API_TOKEN }}
      BINDING_INVITE_CODE_ENCRYPTION_KEY: ${{ secrets.BINDING_INVITE_CODE_ENCRYPTION_KEY }}
      ADMIN_BATTLETAG: ${{ secrets.ADMIN_BATTLETAG }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/action-setup@v6
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate production resources
        shell: bash
        run: |
          set -euo pipefail
          test -n "$CLOUDFLARE_API_TOKEN"
          test -n "$CLOUDFLARE_ACCOUNT_ID"
          test -n "$CLOUDFLARE_ZONE_ID"
          test -n "$QQBOT_API_TOKEN"
          test -n "$BASTION_BUILD_TOKEN"
          test -n "$QQBOT_POLICY_WEBHOOK_URL"
          test -n "$QQBOT_POLICY_WEBHOOK_SECRET"
          test -n "$OCRKIT_API_TOKEN"
          test -n "$BINDING_INVITE_CODE_ENCRYPTION_KEY"
          test -n "$ADMIN_BATTLETAG"
          if [[ "$ADMIN_BATTLETAG" =~ ^(.+)#([0-9]{1,10})$ ]]; then
            admin_player_name="${BASH_REMATCH[1]}"
            admin_player_id="${BASH_REMATCH[2]}"
          else
            echo "ADMIN_BATTLETAG must use the format Name#1234" >&2
            exit 1
          fi
          admin_player_name_normalized="${admin_player_name,,}"
          admin_player_name_normalized_sql="${admin_player_name_normalized//\'/\'\'}"
          echo "::add-mask::$admin_player_id"
          echo "::add-mask::$admin_player_name_normalized_sql"
          {
            echo "ADMIN_PLAYER_ID=$admin_player_id"
            echo "ADMIN_PLAYER_NAME_NORMALIZED_SQL=$admin_player_name_normalized_sql"
          } >> "$GITHUB_ENV"
          test "$(grep -c 'database_id = "00000000-0000-0000-0000-000000000000"' wrangler.toml)" -eq 0
          test "$(grep -c 'database_name = "owbastion-codes-prod"' wrangler.toml)" -eq 1
          test "$(grep -c 'id = "00000000-0000-0000-0000-000000000000"' wrangler.toml)" -eq 0
          test "$(grep -c 'OCRKIT_BASE_URL = "https://ocr.owbastion.com"' wrangler.toml)" -eq 1

      - name: Apply staging D1 migrations
        run: pnpm exec wrangler d1 migrations apply DB --remote --config wrangler.staging-d1.toml

      - name: Verify staging D1 submission and review schema
        run: bash tools/verify-staging-d1.sh

      - name: Ensure Queue resources exist
        shell: bash
        run: |
          set -euo pipefail
          queues="$(pnpm exec wrangler queues list)"
          for queue in owbastion-ocr owbastion-qq-policy owbastion-qq-policy-dlq; do
            if grep -Fq "$queue" <<<"$queues"; then echo "$queue already exists"; else pnpm exec wrangler queues create "$queue"; fi
          done

      - name: Apply remote D1 migrations
        run: pnpm exec wrangler d1 migrations apply owbastion-codes-prod --remote

      - name: Validate administrator account
        shell: bash
        run: |
          set -euo pipefail
          result="$(pnpm exec wrangler d1 execute owbastion-codes-prod --remote --json --command "SELECT COUNT(*) AS count FROM player_accounts WHERE normalized_player_name = '$ADMIN_PLAYER_NAME_NORMALIZED_SQL' AND player_id = '$ADMIN_PLAYER_ID'")"
          test "$(printf '%s' "$result" | jq -r '.[0].results[0].count')" = "1"

      - name: Bootstrap administrator
        shell: bash
        run: pnpm exec wrangler d1 execute owbastion-codes-prod --remote --command "UPDATE player_accounts SET is_admin = 1, updated_at = CAST(strftime('%s','now') AS INTEGER) * 1000 WHERE normalized_player_name = '$ADMIN_PLAYER_NAME_NORMALIZED_SQL' AND player_id = '$ADMIN_PLAYER_ID'"

      - name: Update Worker service secrets
        shell: bash
        run: |
          printf '%s' "$QQBOT_API_TOKEN" | pnpm exec wrangler secret put QQBOT_API_TOKEN
          printf '%s' "$BASTION_BUILD_TOKEN" | pnpm exec wrangler secret put BASTION_BUILD_TOKEN
          printf '%s' "$QQBOT_POLICY_WEBHOOK_URL" | pnpm exec wrangler secret put QQBOT_POLICY_WEBHOOK_URL
          printf '%s' "$QQBOT_POLICY_WEBHOOK_SECRET" | pnpm exec wrangler secret put QQBOT_POLICY_WEBHOOK_SECRET
          printf '%s' "$OCRKIT_API_TOKEN" | pnpm exec wrangler secret put OCRKIT_API_TOKEN
          printf '%s' "$BINDING_INVITE_CODE_ENCRYPTION_KEY" | pnpm exec wrangler secret put BINDING_INVITE_CODE_ENCRYPTION_KEY

      - name: Deploy Worker
        run: pnpm exec wrangler deploy --var DEPLOYMENT_REVISION:${GITHUB_SHA} --var PUBLIC_HTTP_CACHE_ENABLED:true

      - name: Deploy API Shield endpoint operations
        run: pnpm deploy:api-endpoints

      - name: Publish API URL
        run: echo "::notice title=Cloudflare Worker deployed::https://api.owbastion.com"

      - name: Verify OCRKit health
        run: curl --fail --silent --show-error https://ocr.owbastion.com/health

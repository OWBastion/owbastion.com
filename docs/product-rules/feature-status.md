# Feature Status Matrix

This is the single source of truth for the implementation status of platform
capabilities. Other documents may explain boundaries and contracts, but must
not maintain a separate feature-status list.

## Status definitions

| Status | Meaning |
| --- | --- |
| `planned` | The capability is part of the intended product scope, but the repository does not contain the implementation. |
| `coded` | The capability has source implementation and relevant local unit or contract coverage, but no complete integration evidence is recorded. |
| `integration-tested` | The capability has an end-to-end or cross-boundary test using the applicable local fakes or test services. |
| `production-verified` | The capability has traceable evidence from the deployed production path, such as a recorded health check or end-to-end verification. |

CI success, image publication, or deployment completion alone does not qualify
as `production-verified`.

## Current matrix

| Capability | Status | Owner and boundary | Evidence | Next stage |
| --- | --- | --- | --- | --- |
| QQ binding and verification | `coded` | Administrator invitations resolve the target BattleTag; clean first bindings auto-activate after one QQ confirmation, while conflicts remain reviewable; expired confirmation codes regenerate from the original invitation link; maintainers can update a player's BattleTag display name while keeping the numeric ID stable; the platform owns player identity, bindings, login attempts, and sessions | `apps/api/src/app.ts`, `packages/auth`, invitation/claim/session tests, `qqbot/src` | Add deployed invitation-to-session verification |
| Portal QQ login, sessions, and player center | `coded` | Portal renders; API owns identity and sessions | Portal/API tests and session contract code | Add deployed browser-flow verification |
| Portal editorial content and Studio publishing | `coded` | Portal owns typed Blog and released Changelog Markdown under `apps/portal/content/`, including the migrated Feishu-derived Blog archive, the 3.0–5.0 Changelog archive, and public list/detail routes; standalone `/studio` is an Admin-gated same-origin Git editor whose server-side credential proxy limits writes to Portal content and media roots. Local editing does not imply the Git → CI/deploy → public-page chain has run | `apps/portal/content.config.ts`, `apps/portal/pages/blog`, `apps/portal/pages/changelog`, `apps/portal/pages/studio.vue`, `apps/portal/server/api/studio`, `apps/portal/tests/e2e/ssr.test.ts`, `apps/portal/pages/editorial.test.ts`, `apps/portal/tests/content`, `apps/portal/tests/studio`, `apps/portal/utils/studio-route.test.ts`, `docs/deployment/portal-editorial-pilot.md`, `docs/deployment/portal-studio.md` | Record production evidence for anonymous Blog/Changelog reads, the Admin → Studio bridge, content-only publish, CI/deploy, and public-page reflection |
| Portal-only screenshot submission and R2 evidence delivery | `coded` | Portal/API own upload sessions and submission state; player reads remain private while maintainer review reads use the configured R2 CDN origin; QQBot does not create submissions or persist attachments | `apps/portal/pages/submissions`, `apps/api/src/app.ts`, upload contracts, API and Portal tests | Add R2-backed submission integration evidence |
| Verified mastery-run ledger and private player read | `coded` | D1 owns immutable accepted run facts, active same-player run-code uniqueness, invalidation/restoration lifecycle, deterministic XP v1 snapshots, and rebuildable map projections. `/v1/me/mastery` returns only the signed-in player's current-default aggregate unless a revision is explicitly selected, plus bounded private history; an invalidated history row carries only its status, never a reason. It omits run codes, source submissions, account/QQ identity, OCR/evidence, risk, audit, and rule-snapshot fields | `migrations/0058_mastery_runs.sql`, `migrations/0061_gameplay_revisions.sql`, `packages/domain/src/mastery.ts`, `packages/database/src/mastery.test.ts`, `packages/contracts/src/index.ts`, `apps/api/src/app.test.ts`, `docs/api/openapi.json` | Complete cross-repository and deployed evidence in #80 |
| Gameplay revisions and revision-scoped progression | `coded` | A stable map has one default and any number of preparing/selectable/historical gameplay revisions. Revision challenge assignments project map-title rules, direct map challenges, map-scoped title challenges, and CLASSIC/PIONEER through the exact contract `gameplayRevisionId`; submission, title-grant, and mastery facts retain immutable revision snapshots. `0062` clears synthetic legacy CLASSIC provenance and derives its version from factual legacy data | `migrations/0061_gameplay_revisions.sql`, `migrations/0062_repair_legacy_classic_revisions.sql`, `packages/domain/src/gameplay-revision.ts`, `packages/database/src/gameplay-revision-migration.test.ts`, `packages/database/src/mastery.test.ts`, `packages/database/src/map-title-rule.test.ts`, `packages/contracts/src/index.ts`, `apps/api/src/app.test.ts` | Add the #90 editor and #91 Bastion runtime projections, then collect deployed evidence |
| Submission-derived mastery outcomes and run-code deduplication | `coded` | The existing submission → private evidence → OCR path derives a mastery run only after platform-owned player, completion, canonical map/difficulty, game-version, layout, confidence, and normalized run-code checks. `submission_outcomes` keeps independent mastery-run, title-grant, and challenge facts; public/player views expose only the safe mastery status and awarded XP, while maintainer views retain conflict/review detail. Reused same-player codes award 0 XP; different players remain independent; legacy title-only evidence remains valid without a mastery run | `migrations/0059_submission_outcomes.sql`, `packages/database/src/index.ts`, `packages/database/src/map-title-rule.test.ts`, `packages/contracts/src/index.ts`, `docs/product-rules/integrations-and-workflows.md` | Complete Bastion/OCRKit compatibility and deployed evidence in #80 |
| Player mastery profiles and submission outcomes | `coded` | The Portal reuses existing map detail, player center, and submission surfaces for the signed-in player: compact per-map XP/counts and personal bests, map-specific bounded run history, and concise independent title/mastery outcomes. Guests never request private mastery data; ordinary player rows omit run codes, evidence, QQ identity, audit, risk, and maintainer-only conflict state/detail | `apps/portal/pages/maps.vue`, `apps/portal/components/maps/MapMasteryProfile.vue`, `apps/portal/components/maps/MasteryRunHistory.vue`, `apps/portal/pages/me.vue`, `apps/portal/pages/submissions/[submissionId].vue`, Portal component tests | Complete local cross-boundary and deployed browser evidence in #80 |
| Maintainer mastery-run inspection and reconciliation | `coded` | Maintainers can filter private verified runs by player, map, difficulty, state, accepted date, origin, and run code; inspect the source submission/evidence route, OCR settlement facts, XP snapshot, map projection, lifecycle, and conflicts; and idempotently keep an original record or invalidate/restore it with audited actor/time/reason. Conflicting screenshots remain separate and continue through the normal reviewed submission path; title-only grant revocation remains independent | `migrations/0060_mastery_run_conflict_resolutions.sql`, `packages/database/src/index.ts`, `apps/api/src/app.ts`, `apps/portal/pages/admin/mastery-runs/index.vue`, `apps/portal/components/admin/AdminMasteryRunDetail.vue`, database/API/Portal tests | Complete deployed maintainer and cross-repository evidence in #80 |
| Mastery cross-boundary local integration and release gate | `integration-tested` | Local D1, R2, Queue, and OCRKit fakes cover authenticated upload through a private object, first acceptance, exact and changed-byte same-code replays, independent players, conflict, invalidation/restoration, mastery-only approval, combined title plus mastery approval, privacy-negative reads, and bounded profile/list queries. Runtime mastery acceptance remains disabled until released game and OCR compatibility are explicitly configured | `packages/database/src/map-title-rule.test.ts`, `packages/database/src/mastery.test.ts`, `packages/domain/src/mastery.ts`, `apps/api/src/worker.test.ts`, `wrangler.toml`, `docs/product-rules/integrations-and-workflows.md` | Record released Bastion/OCRKit compatibility, then capture sanitized deployed API/Queue/R2/OCR evidence and browser verification |
| Player review persistence and domain foundation | `integration-tested` | D1 and the platform domain own event/map review rows, player association, 1–5 ratings, optional comments, anonymous-display preference, lifecycle/audit state, uniqueness, and aggregates; ratings do not modify catalog, map difficulty, event weight, Agents, or Bastion data | `migrations/0057_reviews.sql`, `packages/database/src/schema.ts`, `packages/database/src/index.ts`, `packages/database/src/review.test.ts`, `tools/test-review-migration.sh` | Add deployed moderation evidence |
| Authenticated player review API | `integration-tested` | The Portal session is the only player identity boundary; players can read, create/update, and withdraw their own event/map review through private idempotent API routes, while responses omit account, moderation, and audit fields | `apps/api/src/app.ts`, `packages/contracts/src/index.ts`, `apps/api/src/app.test.ts`, `packages/database/src/review.test.ts`, `docs/api/openapi.json`, `apps/portal/composables/usePlayerReview.ts`, `apps/portal/components/reviews/PlayerReviewPanel.vue`, `apps/portal/components/reviews/PlayerReviewPanel.test.ts`, `apps/portal/components/events/EventDirectory.vue`, `apps/portal/components/events/EventDirectory.test.ts` | Add deployed session/write evidence |
| Public review summaries and comments API | `integration-tested` | Public callers can retrieve single or bounded batch summaries and deterministic paginated comments for event/map targets; aggregates exclude withdrawn and invalidated reviews, hidden comments remain rating-only, anonymous authors are omitted, and non-anonymous comments expose only the public display name; reads remain private and uncached | `packages/domain/src/index.ts`, `packages/database/src/index.ts`, `packages/database/src/review.test.ts`, `packages/contracts/src/index.ts`, `apps/api/src/app.ts`, `apps/api/src/app.test.ts`, `docs/api/openapi.json`, `apps/portal/composables/usePlayerReview.ts`, `apps/portal/composables/useReviewSummaries.ts`, `apps/portal/components/reviews/ReviewSummary.vue`, `apps/portal/components/reviews/ReviewSummaryBadge.vue`, `apps/portal/components/reviews/PlayerReviewPanel.test.ts`, `apps/portal/components/events/EventDirectory.test.ts` | Add deployed read evidence |
| Maintainer player-review moderation | `integration-tested` | Authenticated maintainers can page and filter review records, inspect the real submitting player and audit context, hide/restore comments, and invalidate/restore whole reviews; optional reasons are retained in audit payloads, idempotent transitions preserve the review row, hidden comments remain rating-only, and invalidated reviews leave public/player projections through the existing D1 boundary | `packages/contracts/src/index.ts`, `packages/domain/src/index.ts`, `packages/database/src/index.ts`, `packages/database/src/review.test.ts`, `apps/api/src/app.ts`, `apps/api/src/app.test.ts`, `apps/portal/pages/admin/player-reviews/index.vue`, `apps/portal/components/admin/AdminReviewDetail.vue`, `apps/portal/components/AppHeader.vue`, `docs/api/openapi.json` | Add deployed event/map moderation evidence |
| Map and achievement challenge catalogs | `coded` | Platform owns current map, title, and challenge metadata; maintainers can create global or map-scoped title challenges; Bastion reads it through the Agents API for builds | Catalog API, admin create/edit Portal, challenge-map migration, database and API tests | Add deployed catalog query verification |
| Map title rule model | `coded` | Platform owns a reusable `map_title_rules` entity (one row per kind) with explicit map-variant constraints, per-map exceptions, and map-aware compatibility IDs; `PIONEER` is exception-only for map reworks/new-map launch windows; `0049` migrates standard rules, `0050` restores the source CLASSIC challenge, `0052` persists its OCR variant, `0054` materialises variant challenges into the rule projection, and `0056` repairs the Pioneer default scope and affected automatic grants without changing public IDs | `migrations/0048_map_title_rules.sql`, `migrations/0049_migrate_standard_map_title_rules.sql`, `migrations/0050_migrate_classic_map_title.sql`, `migrations/0052_add_title_challenge_map_variant.sql`, `migrations/0054_materialise_variant_map_title_rules.sql`, `migrations/0056_restrict_pioneer_map_title_scope.sql`, `tools/map-title-rule-reconciliation.sql`, `packages/database/src/schema.ts` (mapTitleRules/mapTitleRuleExceptions/mapTitleRuleCompat), `packages/database/src/index.ts` (map-aware resolution, Pioneer scope guard, immutable variant snapshots, review/grant audit), `packages/database/src/map-title-rule.test.ts` | Apply `0056`, archive pre/post reconciliation, and validate Bastion build-token projections before legacy cleanup |
| Random-event directory and CSV import | `coded` | Platform owns current event metadata, challenge links, CSV validation, and atomic import audit; Bastion reads it through the Agents API for builds | `apps/portal/pages/events`, `apps/portal/pages/admin/events.vue`, event contracts/database code, event tests | Add deployed directory query verification |
| Public Agents content API | `coded` | Public read-only projection over published events, maps, achievements, titles, and public title-holding facts; ordinary requests omit numeric player IDs, while requests carrying the Bastion build token may receive current player display names, stable game player IDs, active title keys, and required map scope; no QQ, submission, review-source, time, or audit data | `/v1/agents/*`, `apps/api/src/public-cache.ts`, Agent contracts/domain/database projections, API tests | Add deployed query and Cache API MISS → HIT verification |
| Achievement title icon management | `coded` | Platform owns title metadata, optional public icon URLs, and private uploaded icon objects; Bastion reads the public metadata projection | `apps/api/src/app.ts`, `apps/portal/pages/admin/achievements.vue`, title icon migrations and tests | Add production asset-path verification |
| QQ group lifecycle and single active group | `coded` | Platform owns group state and command policy; QQBot reports lifecycle events and applies the platform snapshot | `migrations/0026_single_active_qq_group.sql`, group API/Portal code, `qqbot/src`, lifecycle tests | Add deployed group promotion and disconnect verification |
| QQ group policy Outbox push and retry repair | `coded` | Platform records policy changes and delivers signed Queue events; QQBot acknowledges and refreshes its local snapshot | `migrations/0030_qq_group_policy_outbox.sql`, `apps/api/src/worker.ts`, `qqbot/src`, Queue and webhook tests | Add deployed policy delivery and repair verification |
| Global QQ member identity | `coded` | The platform binds one `(provider, memberOpenId)` identity across groups; group OpenIDs remain context and policy scope | `migrations/0029_global_qq_member_open_id.sql`, database/domain code, migration and API tests | Add deployed cross-group identity verification |
| Map-challenge OCR matching | `coded` | OCRKit recognizes; platform compares structured evidence with the selected map, explicit variant constraint, completion, and player; a recognized harder difficulty satisfies an easier target, `普通` normalizes to `一般`, and `地狱：`-prefixed labels count as `地狱`; automatic matching requires reliable map-name evidence and evaluates map challenges only for the recognized map; missing or weak classic-variant evidence is rejected | `packages/database/src/ocr-match.ts`, `packages/database/src/ocr-response.ts`, OCR match/quality tests, upload confirmation and Queue workflow code | Add full submission-to-match integration evidence |
| Automatic screenshot decisions, title grants, and spot checks | `coded` | Platform derives candidates from the current active challenge catalog, restricts map candidates to the OCR current map, auto-approves one complete high-confidence rewardable match at the highest recognized difficulty, and issues covered lower map rewards in the same decision; otherwise routes ambiguity/low confidence to maintainer review or mismatch back to resubmission; approval and automatic Grants are one D1 batch, with immutable rule/match audit and deterministic optional spot checks | `packages/database/src/ocr-auto-match.ts`, `packages/database/src/index.ts`, `migrations/0055_submission_automatic_decisions.sql`, spot-check API/Portal surface and tests | Add local fake OCRKit/R2/Queue end-to-end evidence and production sampling verification |
| OCR Queue delivery and retry handling | `coded` | Worker owns Queue orchestration and retry state; OCRKit owns recognition | `apps/api/src/worker.ts`, Queue consumer tests, deployment queue setup | Add Queue-to-OCRKit integration evidence |
| OCRKit service authentication and production deployment | `coded` | OCRKit authenticates recognition requests with its service token; the platform Worker is the only caller; deployment uses pinned images and Cloudflare Tunnel | `ocrkit/app/api/routes_ocr.py`, `ocrkit/docker-compose.production.yml`, `ocrkit/docs/production-deployment.md`, `docs/deployment/api-github-actions.md` | Record deployed authenticated OCR request and health evidence |
| Maintainer submission review | `coded` | Platform maintainer surface owns review decisions; OCR is evidence only | Review API, Portal admin tests, contract and database code | Add review end-to-end integration evidence |
| Historical title migration and grants | `coded` | Platform owns historical title records; administrators can explicitly authorize selected unclaimed historical records on a binding invitation, and approved bindings create or reuse map-aware historical Grants idempotently while preserving manual repair paths | Generic Grant API, binding invitation authorization, automatic post-binding migration, `/admin/titles`, `/admin/grants`, audit/idempotency code, binding-flow tests | Add production operational verification |
| New title issuance | `coded` | Platform owns the internal title Grant created atomically with maintainer approval; Bastion release/build state is out of scope | Submission review transaction, generic Grant migration, API and Portal response | Add complete local integration evidence |

No capability is currently marked `production-verified`: this repository does
not contain traceable production end-to-end evidence for these user-facing
chains. Update this table only when such evidence is recorded.

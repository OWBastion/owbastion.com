export type EventChallenge = { challengeId: string; family: "map"; gameplayRevisionId: string; name: string; mapId?: string } | { challengeId: string; family: "achievement"; titleName: string };
export type EffectAnnotation = { tag: string; term: { key: string; nameZh: string; aliases: string[]; category: string; summary: string; definition: string; rules: string[]; sourceVersion: string } };
export type RandomEvent = { eventId: string; name: string; category: string; rarity: string; description: string; durationSeconds: number | null; cooldownSeconds: number | null; weight: number | null; gameVersion: string; effectTags: string[]; effectAnnotations: EffectAnnotation[]; releaseStatus: "development" | "implemented" | "removed"; archived: boolean; challenges: EventChallenge[] };

# Claude Code Instructions

@AGENTS.md

## Claude Code Workflow

- `AGENTS.md` is the single source of truth for project conventions, architecture boundaries, development workflow, and completion criteria.
- When using Claude Code, read only the task-routed documents from `AGENTS.md` before editing.
- Add content here only when it is specific to Claude Code capabilities or interaction; update `AGENTS.md` first when project rules change.
